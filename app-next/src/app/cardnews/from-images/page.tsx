import '@/lib/artbrows/tokens.css';
import '../cardnews.css';
import FromImagesWizard from './FromImagesWizard';

export const metadata = {
  title: 'ARTbrows · 이미지로 시작 (3-step)',
};

export default function FromImagesPage() {
  return <FromImagesWizard />;
}
